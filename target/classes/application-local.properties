# --- Local Development Profile ---

# --- Local Development Profile ---

spring.datasource.url=jdbc:mysql://localhost:3307/currency_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Devin@07
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# This tells Hibernate to create/update tables
spring.jpa.hibernate.ddl-auto=update
# This explicitly sets the MySQL dialect to fix the 'Dialect' error
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
